cat("===== Setting my own repository path =====")
r = getOption("repos") # hard code the UK repo for CRAN
r["CRAN"] = "https://cran.us.r-project.org"
options(repos = r)
rm(r)

library("prettydoc")
